import matplotlib.pyplot as plt
import numpy as np


if __name__ == '__main__':
    pass

    # file = './data/worthTrend/110005.txt'
    # with open(file, 'r', encoding='utf-8') as f:
    #     data = f.readline()
    #     points = data.strip(' ').split(' ')
    #
    # worthes = []
    # for point in points:
    #     if not point:
    #         continue
    #     worthes.append(float(point.split('-')[1]))
    #
    #
    # yy = np.array(worthes)
    # res = (yy[1:] - yy[0:-1]) / yy[0:-1]
    #
    # fig, ax = plt.subplots(2)
    # ax[0].plot(worthes)
    # ax[1].plot(res)
    #
    # plt.show()










